import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["png.pngtree.com", "images.unsplash.com", "plus.unsplash.com", "upload.wikimedia.org", "avatars.githubusercontent.com"]
  }
};

export default nextConfig;
